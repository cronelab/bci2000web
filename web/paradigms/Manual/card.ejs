<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">Manual Recording</h3>
  </div>
  <div class="panel-body">

    <p>Manually record a data file.</p>

    <div class="input-group">
      <span class="input-group-addon">Task Label</span>
      <input id="manual-taskLabel" type="text" class="form-control" placeholder="e.g., FooBar">
      <span class="input-group-btn">
        <button class="btn btn-info" type="button" onclick="runManual()">Start</button>
      </span>
    </div><!-- /input-group -->

    <script>
      function runManual() {
          var taskLabel = $( '#manual-taskLabel' ).val();
          if ( taskLabel.length == 0 ) {
              alert( 'You must enter a task name for the recording.' );
          } else {
              window.location = "<%- cardRoot; %>/index.html?name=" + encodeURIComponent( taskLabel ) + '&block=0';
          }
      }
    </script>

  </div>
</div>